spring.application.name=scheduler

# Database Configuration (PostgreSQL)
spring.datasource.url=jdbc:postgresql://localhost:5432/scheduler
spring.datasource.username=scheduler_user
spring.datasource.password=scheduler_password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Security (Clerk Configuration)
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://caring-eel-28.clerk.accounts.dev

# OAuth2 Client Configuration (For Thymeleaf Login Flow)
spring.security.oauth2.client.registration.clerk.client-id=WBinG1s38y6VPNDK
spring.security.oauth2.client.registration.clerk.client-secret=hpmh0K6eksqYcYo7yIP9ylFHqpMVGVpQ
spring.security.oauth2.client.registration.clerk.scope=openid,email,profile
spring.security.oauth2.client.registration.clerk.authorization-grant-type=authorization_code
# Try sending credentials in POST body instead of Basic Auth header
spring.security.oauth2.client.registration.clerk.client-authentication-method=client_secret_post

# Debug logging for Security to see exact errors
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.security.oauth2=DEBUG
logging.level.org.springframework.web.client.RestTemplate=DEBUG
spring.security.oauth2.client.registration.clerk.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}
spring.security.oauth2.client.provider.clerk.issuer-uri=https://caring-eel-28.clerk.accounts.dev
# Use user-name-attribute=sub if you want the ID, or other claim
spring.security.oauth2.client.provider.clerk.user-name-attribute=email